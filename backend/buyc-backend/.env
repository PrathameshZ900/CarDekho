
PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/pmdb
# JWT_SECRET=your_jwt_secret

const crypto = require('crypto');

// Generate a 32-byte (256-bit) secret
const JWT_SECRET = crypto.randomBytes(32).toString('hex');
console.log(JWT_SECRET);